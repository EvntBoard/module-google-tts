name: Module Google TTS

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*"

jobs:
  module-automation:
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v2

      - name: Install Node.js, NPM and Yarn
        uses: actions/setup-node@v1
        with:
          node-version: 12

      - name: Build
        run: |
          yarn
          yarn build

      - name: Pack Linux
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          cd dist
          npx bestzip ../module-google-tts-linux.zip .

      - name: Pack Windows
        if: startsWith(matrix.os, 'windows')
        run: |
          cd dist
          npx bestzip ../module-google-tts-windows.zip .

      - name: Pack Macos
        if: startsWith(matrix.os, 'macos')
        run: |
          cd dist
          npx bestzip ../module-google-tts-macos.zip .

      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          draft: true
          files: |
            *.zip